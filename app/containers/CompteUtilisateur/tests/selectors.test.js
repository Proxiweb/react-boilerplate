import {
  selectCompteUtilisateur,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectCompteUtilisateur();

describe('selectCompteUtilisateur', () => {

});
