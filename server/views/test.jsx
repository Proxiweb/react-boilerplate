/* eslint-disable */
const React = require('react');

const Facture = React.createClass({
  // eslint-disable-line
  render: function() {
    return <h1>{this.props.message}</h1>;
  },
});

module.exports = Facture;
